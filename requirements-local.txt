# ML Platform — full local install
#
#     pip install -r requirements-local.txt
#     streamlit run app.py
#
# This is the superset. `requirements.txt` is the slimmed-down version used
# for Streamlit Community Cloud, which has a disk quota the CUDA wheels
# exceed.

# ---- core: required, the platform will not start without these -------
streamlit>=1.40
numpy>=1.26
pandas>=2.0
plotly>=5.20
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8

# ---- deep learning: required for chapters 10-19 ----------------------
# CPU only (and what the cloud deployment uses):
tensorflow-cpu>=2.16,<2.22
#
# For a GPU instead, drop the line above and install one of:
#   Linux / WSL2 + NVIDIA :  pip install "tensorflow[and-cuda]"
#   macOS Apple silicon   :  pip install tensorflow tensorflow-metal
#   Windows               :  no GPU support since TF 2.11 — use WSL2

# ---- optional -------------------------------------------------------
# Each affects exactly one lab. That lab detects the absence, prints the
# install command, and continues with a fallback.
statsmodels>=0.14        # SARIMA comparison, §15.3
sympy>=1.12              # symbolic differentiation, §B.1
transformers>=4.40       # Hugging Face pipeline, §16.9
tensorboard>=2.16        # the TensorBoard callback, §10.7
keras-tuner>=1.4         # hyperparameter search, §10.8
tensorflow-datasets      # a few dataset loaders

# ---- development ----------------------------------------------------
# Only needed to regenerate the README screenshots.
# playwright>=1.40       # then: python -m playwright install chromium
