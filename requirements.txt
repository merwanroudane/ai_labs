# ML Platform — deployment requirements (Streamlit Community Cloud)
#
# Deliberately lean. On Linux the plain `tensorflow` wheel drags in the whole
# NVIDIA CUDA stack (nvidia-cublas-cu12, nvidia-cudnn-cu12, …), several GB of
# wheels that blow Streamlit Cloud's disk quota and abort the install.
# `tensorflow-cpu` is the same library without those, and Cloud has no GPU
# anyway.
#
# For a full local install, including the optional extras, use:
#     pip install -r requirements-local.txt

streamlit>=1.40
numpy>=1.26
pandas>=2.0
plotly>=5.20
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8

# deep learning — chapters 10-19
tensorflow-cpu>=2.16,<2.22

# the only optional package small enough to be worth shipping (§15.3 SARIMA)
statsmodels>=0.14
