# ML Platform — interactive Hands-On Machine Learning course
#
#   pip install -r requirements.txt
#   streamlit run app.py

# ---- core: required, the platform will not start without these -------
streamlit>=1.40
numpy>=1.26
pandas>=2.0
plotly>=5.20
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8

# ---- deep learning: required for chapters 10-19 ----------------------
# Linux/WSL with an NVIDIA GPU:  pip install "tensorflow[and-cuda]"
# macOS on Apple silicon:        pip install tensorflow tensorflow-metal
tensorflow>=2.16

# ---- optional -------------------------------------------------------
# Every one of these affects exactly one lab, which detects its absence,
# prints the install command, and continues with a fallback.
statsmodels>=0.14        # SARIMA comparison, section 15.3
sympy>=1.12              # symbolic-differentiation demo, section B.1
transformers>=4.40       # Hugging Face pipeline, section 16.9
tensorboard>=2.16        # the TensorBoard callback, section 10.7
keras-tuner>=1.4         # hyperparameter search, section 10.8
tensorflow-datasets      # a few dataset loaders
